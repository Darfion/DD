overwrite: true
schema: "http://localhost:8000/graphql"
generates:
  src/toolses/generated/typescriptResolvers.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  src/toolses/generated/schema-ast.graphql:
    plugins:
      - schema-ast
